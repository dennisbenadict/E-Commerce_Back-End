services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong!Pass123
    ports:
      - "1433:1433"
    networks:
      - ecommerce-net
    volumes:
      - sql_data:/var/opt/mssql

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"      
      - "15672:15672"    
    networks:
      - ecommerce-net

  authservice:
    build:
      context: .
      dockerfile: AuthService/AuthService.Api/Dockerfile
    container_name: authservice
    depends_on:
      - sqlserver
      - rabbitmq
    ports:
      - "5067:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=AuthDb;User Id=sa;Password=YourStrong!Pass123;TrustServerCertificate=True;
      - Jwt__Key=THIS_IS_A_LONG_SUPER_SECRET_KEY_1234567890_ABCDEF
      - Jwt__Issuer=authservice
      - Jwt__Audience=auth_clients
      - RabbitMq__Host=rabbitmq
      - RabbitMq__User=guest
      - RabbitMq__Password=guest
    networks:
      - ecommerce-net

  productservice:
    build:
      context: .
      dockerfile: ProductService/ProductService.Api/Dockerfile
    container_name: productservice
    depends_on:
      - sqlserver
      - authservice
      - rabbitmq
    ports:
      - "5214:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=ProductDb;User Id=sa;Password=YourStrong!Pass123;TrustServerCertificate=True;
      - Jwt__Key=THIS_IS_A_LONG_SUPER_SECRET_KEY_1234567890_ABCDEF
      - Jwt__Issuer=authservice
      - Jwt__Audience=auth_clients
      - RabbitMq__Host=rabbitmq
      - RabbitMq__User=guest
      - RabbitMq__Password=guest
    networks:
      - ecommerce-net

  userservice:
    build:
      context: .
      dockerfile: UserService/UserService.Api/Dockerfile
    container_name: userservice
    depends_on:
      - sqlserver
      - authservice
      - rabbitmq
    ports:
      - "5031:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=UserDb;User Id=sa;Password=YourStrong!Pass123;TrustServerCertificate=True;
      - Jwt__Key=THIS_IS_A_LONG_SUPER_SECRET_KEY_1234567890_ABCDEF
      - Jwt__Issuer=authservice
      - Jwt__Audience=auth_clients
      - RabbitMq__Host=rabbitmq
      - RabbitMq__User=guest
      - RabbitMq__Password=guest
    networks:
      - ecommerce-net

  gateway:
    build:
      context: .
      dockerfile: Gateway/Gateway.Api/Dockerfile
    container_name: gateway
    depends_on:
      - authservice
      - userservice
      - productservice
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - Jwt__Key=THIS_IS_A_LONG_SUPER_SECRET_KEY_1234567890_ABCDEF
      - Jwt__Issuer=authservice
      - Jwt__Audience=auth_clients
    networks:
      - ecommerce-net

  frontend:
    build:
      context: ../../../.vscode/E-Commerce/ecommerce
      dockerfile: Dockerfile
    container_name: frontend
    depends_on:
      - gateway
    ports:
      - "4200:80"
    networks:
      - ecommerce-net

networks:
  ecommerce-net:
    driver: bridge

volumes:
  sql_data:



